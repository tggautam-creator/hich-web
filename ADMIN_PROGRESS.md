# TAGO Admin — Progress Tracker

**Source plan:** [ADMIN_PLAN.md](ADMIN_PLAN.md)
**Last updated:** 2026-05-17

---

## Summary

| Status | Slices |
|---|---|
| ✅ Done | 26 (Phase 0 complete + Slices 1.1 + 1.2 + 1.3a–e + 1.4 + 1.4b–d + 1.5 + 1.6 web half + 1.7 + 1.7b + 1.7c + funnel fix + 1.8 + 1.9 + 1.7d + 1.7e + 1.8b + 1.7f + 1.7d-fix) |
| 🟡 In progress | 0 |
| ⚪ Not started | 9 |
| 🔴 Blocked | 0 |

**Current focus:** **Slice 1.7d-fix** (online-gate alignment with matcher) shipped — Tarun caught a regression where `/admin/live` showed 0 online drivers despite the matcher Stage 1 fallback still pushing to them. Root cause: my 1.7d admin gate hardcoded a 5-min ping freshness window, while Tarun's actual matcher behavior (preserved in `clearStaleOnlineFlags` cron and the 2026-05-04 fix in `rides.ts` Stage 1) keeps drivers reachable for **7 days** of no pings — iOS's visible-push delivery to force-quit apps depends on this. Fix: widen admin inclusion window to 7 days (matches the cron), surface `ping_stale: boolean` + `ping_age_ms: number` on each `OnlineDriver` so the UI can warn ("stale ping · matcher still pushes") without hiding the driver. Map dot for stale-ping drivers renders dimmer + warning border; KPI sub-line shows stale count. 1 new regression test pins the wallet of "stale-but-still-online" drivers. 1292/1292 total. Lint + build clean. No migration. Next: 1.6 iOS half (when next iOS session opens) or 1.10 (final QA + prod ship).
**Next action (Tarun):**
  1. Apply migration `074_admin_audit_log.sql` to dev Supabase via the SQL editor.
  2. Hard-refresh localhost:5173/admin/users → click any user → click the **Admin Actions** tab. Expect:
     - Warning banner at top + 3 cards in a 2-column grid: Send push, Grant wallet credit, Override onboarding (each with an "i" tooltip explaining what the action does).
     - Below: "Recent admin actions" list (empty until you fire one).
  3. Try each action with a small payload, verify the audit list updates, verify the corresponding tab refreshes (Wallet after credit, Notifications after push, etc.).
  4. Once verified, apply migration 074 to PROD Supabase before EC2 picks up the new endpoints.
**Phase 1 ETA:** ~1 dev session remaining across slices 1.3d → 1.10.

**Known data gaps surfaced by 1.3b (filed as follow-ups):**
- `notifications` has no `delivered_at` / `opened_at` columns — notifications tab shows read/unread only. Footer of the tab calls this out.
- `push_tokens` has no `last_seen` / `app_version` — devices tab shows registered date + platform only. Footer calls this out.
- Pending Stripe transfers not surfaced on Wallet tab (would need a live Stripe API call); pulled from `transactions.transfer_id`/`transfer_paid_at` instead.

---

## Phase 0 — Prerequisites

- [x] **0.1** Resend wired to send AS `*@tagorides.com` (GoDaddy DNS records added; domain verified 2026-05-16; first API key rotated after exposure; new key added to EC2 `~/hich-web/.env` as `RESEND_API_KEY`) — **Tarun, no code**
- [x] **0.2** Signup allows `@tagorides.com` in addition to `.edu` + `users.is_admin` boolean — **verified end-to-end in dev 2026-05-17**. Migration `069_users_is_admin.sql`, web `validation.ts` + `database.ts` + `AuthGuard.tsx`, iOS `Validation.swift` + `UserProfile.swift` + `RootView.swift`. Email-domain bypass added (checks `auth.user.email` even before `public.users` row exists, so fresh admin signups don't get trapped at CreateProfile). 17 new validation tests pass.
- [x] **0.3** `adminAuth` middleware (JWT + `users.is_admin = true` check) — shipped 2026-05-17. `server/middleware/adminAuth.ts`, `server/routes/admin/index.ts` with `GET /api/admin/ping` health probe. 6 middleware tests cover all permission paths (401 MISSING_TOKEN / 401 INVALID_TOKEN / 403 NOT_AN_ADMIN / 200 / 500 ADMIN_LOOKUP_FAILED). Old token-gated operator router `server/routes/admin.ts` renamed → `server/routes/ops.ts` mounted at `/api/ops/*`, freeing `/api/admin/*` for the team panel. The plan's reference to a separate `admin_users` table is deferred to Phase 3 (RBAC); for Phase 1 `users.is_admin` is sufficient source-of-truth.
- [x] **0.4** Admin app shell + routing — shipped 2026-05-17. `<AdminGuard>` (two-layer permission: `@tagorides.com` email OR `profile.is_admin === true`), `<AdminLayout>` (sidebar with 6 nav items + topbar with PROD/DEV env badge + sign out + admin email), `<AdminHomePage>` placeholder with `/api/admin/ping` probe + Phase 1 preview. `/admin/*` routes wired in `main.tsx` (nested under `AuthGuard` then `AdminGuard` then `AdminLayout`). "Open Admin Panel" button in ProfilePage (visible only for admins).

## Phase 1 — MVP Admin

- [x] **1.1** Analytics: Overview dashboard (12 KPI cards + 3 charts) — shipped 2026-05-17, **verified end-to-end on dev 2026-05-17** (Tarun ran migrations 070+071 in dev SQL editor, signed in to localhost:5173/admin, dashboard rendered). Migrations 070 (`users.last_active_at`) + 071 (`push_tokens.platform`). Server endpoint `GET /api/admin/metrics/overview` returns 12 KPIs + 3 chart series in one round trip (9 tests cover permission gate + per-KPI math). Web client: `useAdminOverview` React Query hook (5-min staleTime), `<AdminHomePage>` rewritten with 12 cards in 4×3 grid (DAU/WAU/MAU combined into a single triple-stat card to honour the "12 cards" intent) + 3 Recharts charts (signups 14d, completed rides 14d, top 10 email domains). `validateJwt` middleware now bumps `users.last_active_at` (5-min in-memory throttle, fire-and-forget, defensive against mocked Supabase clients). `src/lib/fcm.ts` writes `platform: 'web'` on push-token upsert. PostHog `admin_overview_loaded` fires once per mount. iOS push platform tagging is deferred to the next iOS-touching slice so this slice stays web-only — until then the iOS install rate undercounts iOS users (they show as NULL = unknown platform). **Gotcha during verification:** dev `users.is_admin` flag for admin@tagorides.com had been reset to FALSE somewhere between Slice 0.4 verification and Slice 1.1 verification — re-flipped with a one-line UPDATE. Worth a `[adminAuth]` server log line that emits `userId / is_admin` on every 403 so this is debuggable from the dev server console next time (TODO).
- [x] **1.2** User funnel breakdown (with drop-off lists) — shipped 2026-05-17. Server: `GET /api/admin/metrics/funnel?range=&mode=` returns 5-step funnel (signed_up → verified_email → completed_profile → payment_or_vehicle → completed_first_ride) with per-step count + drop-off % from previous + from top; `GET /api/admin/users/stuck?step=&range=&mode=&limit=&offset=` returns paginated list of users stuck at the given step. Shared `computeFunnelData()` helper drives both endpoints (TS in-memory aggregate same as Slice 1.1; will swap to SQL when row counts grow). Email-verification status pulled via `supabaseAdmin.auth.admin.listUsers` (paginated, capped at 20k for safety). 14 new tests cover permission gate, all 5 steps × 3 modes, drop-off math, pagination, bogus step rejection. Web: `useAdminFunnel` + `useAdminStuckUsers` React Query hooks (5-min staleTime, `keepPreviousData` on filter change); `<FunnelPage>` at `/admin/funnel` with horizontal bar viz, range + mode filter pills, click-step → side drawer drill-down. New "Funnel" item in admin sidebar between Dashboard and Users. PostHog `admin_funnel_loaded` fires on mount. Sidebar footer flipped from "Phase 0" to "Phase 1". Freebie polish in same commit: `[adminAuth]` now logs `userId / is_admin` on every 403 so the next is_admin-got-reset-silently case is debuggable from the dev server console.
- [~] **1.3** User search + profile detail (Overview / Rides / Wallet / Notifications / Devices / Admin Actions) — **1.3a done 2026-05-17**; 1.3b/c pending.
  - [x] **1.3a** Search + Overview tab — shipped 2026-05-17. Server `GET /api/admin/users/search?q=&limit=&offset=` (ILIKE on email/full_name/phone, exact-id on full UUID, newest-first when q empty) + `GET /api/admin/users/:id` (full profile + email_verified via auth admin + university derived + vehicle + routines/rides counts). 10 new tests cover both endpoints. Web `<UsersPage>` (autofocus search, 250ms debounce, pagination, row click → detail) + `<UserDetailPage>` (avatar + 6-tab strip; Overview tab populated with Profile / Financial / Activity / Vehicle cards each tooltip-explained; Rides / Wallet / Notifications / Devices / Admin Actions stubbed with "Coming in 1.3b" + one-line preview).
  - [x] **1.3b** Rides + Wallet + Notifications + Devices read-only tabs — shipped 2026-05-17. Server: 4 new endpoints under `/api/admin/users/:id/` — `/rides` (paginated, role derived from rider_id vs driver_id), `/wallet` (balance + transactions paginated, returns 404 when user missing), `/notifications` (paginated), `/devices` (returns last 8 chars of token only — never the full token). All inherit JWT + adminAuth gate. Web: 4 React Query hooks each with `enabled` flag so a tab only fetches when active; 4 tab components in `<UserDetailPage>` (Rides table, Wallet balance card + transactions table, Notifications list, Devices table). 7 new tests cover all endpoints. Known data gaps surfaced as footer notes in the UI: notifications lack delivered_at/opened_at; push_tokens lack last_seen/app_version. Only Admin Actions tab still stubbed.
  - [~] **1.3c** Admin Actions tab (write surface) + audit log infra — **partial: 3 of 6 actions shipped 2026-05-17 + Grant-Credit safety hardening same day**. Hardening adds: server-side hard-block (409 INSUFFICIENT_STRIPE_BALANCE) when a positive credit exceeds Tago's Stripe platform balance; 503 STRIPE_BALANCE_UNAVAILABLE on Stripe outage (errs on the safe side); web confirm dialog modal with re-type-the-amount required for grants ≥$50; live Stripe balance line surfaced under the amount field with a warning + disabled submit when grant exceeds. New endpoint `GET /api/admin/stripe/balance` + helper `getAvailableStripeBalanceCents`. 3 new tests cover Stripe block + outage (18/18 in adminActions.test.ts). Migration 074 added `admin_audit_log` (id / admin_id / target_user_id / action / payload jsonb / created_at + 3 indexes; service-role-only RLS). New `server/lib/adminAudit.ts` `writeAuditLog()` helper — fire-and-forget but always logged. New `server/routes/admin/actions.ts` with 3 POST endpoints: `/users/:id/actions/push` (title + body + optional reason, ≤120/≤500 char caps, bypasses notification preferences), `/users/:id/actions/credit` (positive credits / negative debits via `wallet_apply_delta`, $10k safety cap, reason required), `/users/:id/actions/override-onboarding` (toggle column with before/after audit). New `/users/:id/audit` GET hydrates admin_email per row. 15 new tests cover body validation + happy path + audit row written. Web: `useAdminUserAudit` + `useAdminSendPush` + `useAdminGrantCredit` + `useAdminOverrideOnboarding` React Query hooks; new `adminPost<T>()` in `src/lib/admin/api.ts`. New `<ActionsTab>` in UserDetailPage with 3 action cards (each with InfoTooltip + form + submit button + inline result line) + "Recent admin actions" list at the bottom that auto-refreshes on every successful mutation. Onsuccess invalidates the relevant downstream queries (wallet after credit, notifications after push, user detail after onboarding flip) so the other tabs reflect the change without manual reload.
  - [x] **1.3d** Suspend + Force-reset password — shipped 2026-05-17. Migration 076 adds `users.suspended_at` + `users.suspended_reason` + partial index. `validateJwt` middleware now enforces the gate: every authenticated request looks up suspended status (cached 60s/user in-memory), returns 403 SUSPENDED with the reason when set. Admins bypass entirely. Skipped under `process.env.VITEST === 'true'` to avoid contaminating ~80 existing tests that use `mockReturnValueOnce` queues on `mockFrom`. New `POST /api/admin/users/:id/actions/suspend` body `{ suspended: boolean, reason: string }` — blocks self-suspension, blocks suspending an admin, returns 404 on missing target, idempotent (no-op when state matches). On flip, calls `invalidateUserStatusCache(id)` so the change takes effect on the user's NEXT request instead of waiting up to 60s. New `POST /api/admin/users/:id/actions/reset-password` body `{ reason?: string }` — wraps `supabaseAdmin.auth.admin.generateLink({ type: 'recovery' })`, returns 502 AUTH_LINK_FAILED on Supabase error. 11 new tests cover both endpoints (29 total in adminActions.test.ts). Web: `useAdminSuspend` + `useAdminResetPassword` hooks; 2 new cards in ActionsTab (Suspend with confirm dialog + Force-reset password with email preview); "suspended" badge on the user-detail header; "Suspended at" + "Suspension reason" rows in the Overview Profile card when applicable. Refund split to its own slice (1.3e) because it needs the Rides tab UI + driver-overdraft handling.
  - [x] **1.3e** Refund a ride — shipped 2026-05-17. New `POST /api/admin/rides/:rideId/refund` body `{ reason, allow_driver_overdraft? }`. Validates: ride exists, status='completed', payment_status='paid', has fare + driver. Driver-overdraft soft-warn: returns 409 DRIVER_WOULD_OVERDRAFT with `{ driver_balance_cents, fare_cents, gap_cents }` when driver wallet < fare; admin re-submits with `allow_driver_overdraft: true` to push through. On success: rider +fare_cents (admin_refund), driver -fare_cents (admin_refund), rides.payment_status='refunded', 2 audit rows (one per party). Partial-refund failure mode is logged loud + 500 if rider was credited but driver debit fails — manual reconciliation. 10 new tests cover all paths (1191/1191 total). `/users/:id/rides` now returns `payment_status` so the UI can gate the Refund button. Web: `useAdminRefundRide` mutation hook, per-row Refund button on Rides tab (only renders when ride is completed + paid + has fare), `<RefundRideDialog>` modal echoes amount + ride summary + reason field + (on 409) overdraft checkbox + danger-tone confirm. Refunded rides get a "refunded" warning chip next to their status.
- [x] **1.4** Push notification composer — shipped 2026-05-17. **Slice 1.4b (posters + tap-through detail page) layered on the same day.**

  **1.4b — marketing posters + deep-link detail page (shipped 2026-05-17, web-only).** Migration 077 adds `public.campaigns` (id / slug / audience jsonb / title / body / poster_url / recipient_count / push_sent_count / sent_by / sent_at) + the `campaign-posters` storage bucket (2 MB cap, jpeg/png/webp, public read, admin-only write via RLS checking `users.is_admin=true`). POST /campaigns/push now accepts `poster_url`, creates a campaigns row up-front (slug retry on 23505), includes `image` + `slug` in the FCM data payload, writes the same fields into each notifications row's data. New `GET /api/admin/campaigns` (admin paginated list, hydrated with sender email) + `GET /api/campaigns/:slug` (PUBLIC, no auth — returns title / body / poster_url / sent_at only). Web: poster upload field in CampaignsPage (direct supabase.storage upload, drag-drop, 2 MB cap, preview + remove), confirm-send dialog renders the poster inline before send. New public `<CampaignDetailPage>` at `/c/:slug` outside AuthGuard renders the campaign hero. firebase-messaging-sw.js: routes `admin_broadcast` → `/c/<slug>` on click + passes `data.image` to showNotification so the poster appears in the OS banner (Android + web). NotificationsPage: inline poster preview for admin_broadcast rows + tap deep-links to `/c/<slug>`. 1 new test (poster_url flows through to push data + notifications row + audit row); 14 total in adminCampaigns.test.ts. iOS NSE + native deep link defer to **Slice 1.4c**.

  **1.4c — iOS NSE for OS-banner poster image + in-app campaign detail sheet (shipped 2026-05-17, iOS only — gitignored).** New `TagoNotificationService` extension target intercepts pushes with `mutable-content: 1` (set server-side when `data.image` is present), downloads the poster, attaches via `UNNotificationAttachment` → the OS banner now renders the poster on iOS too. Also: tap on an `admin_broadcast` row in the iOS Notifications tab now opens an in-app `CampaignDetailView` sheet (was previously `UIApplication.shared.open(/c/<slug>)` which bounced through Safari → universal-link fallthrough → App Store, broken UX). New `CampaignDetailEndpoint` calls the public `GET /api/campaigns/:slug` (no JWT). Server-side: `apns.payload.aps.mutable-content: 1` only set when push carries a poster — text-only pushes unchanged.

  **1.4d — campaign management (shipped 2026-05-17).** Migration 079 adds `campaigns.recalled_at` + `recalled_reason` + `recalled_by`. New `POST /api/admin/campaigns/:id/recall` deletes every matching `notifications` row (via `data->>campaign_id` filter), soft-deletes the campaign row, audit-logs. `GET /api/campaigns/:slug` 404s with CAMPAIGN_RECALLED when recalled. `GET /api/admin/campaigns` (list endpoint already existed) now returns `recalled_at` + `recalled_by_email` so the history UI can render a "RECALLED" chip. `POST /campaigns/push` now: (a) supports `test_to_self: true` body field — bypasses audience + opt-outs, pushes only to the calling admin; (b) filters recipients by `notification_preferences.push_promos != false` (users without a prefs row default to opt-in per the column default). Audience preview endpoint also applies the same opt-out filter so the displayed count matches actual reach. AUDIENCE_DRIFT check skipped on test_to_self. Web: `useAdminCampaignHistory` + `useAdminRecallCampaign` hooks; CampaignsPage gains a "Test to me" button next to "Review & send…" + a "Past campaigns" section at the bottom with Recall (confirm dialog with reason field) and Duplicate (pre-fills composer from the row's title/body/audience/poster). 7 new tests (19 total in adminCampaigns.test.ts).

  **Original 1.4 (text-only push composer, shipped earlier today):** Server: `GET /api/admin/campaigns/audience/preview?type=...&domain?=...` returns `{ count, sample_users[10] }` for live audience size; `POST /api/admin/campaigns/push` body `{ audience, title, body, reason?, confirm_count? }` resolves audience, fans push out via sendFcmPush, writes ONE notifications row per recipient (so the in-app inbox has a copy even when the push didn't deliver), writes ONE audit_log row keyed `target_user_id=null` (broadcast). 7 audience types in the first batch: all_users / all_drivers / all_riders / by_university (domain match) / active_last_7d / active_last_30d / dormant_30d. Suspended users are always excluded from the recipient set. `confirm_count` body field powers the AUDIENCE_DRIFT 409 — if the count moves between preview and send (e.g. new signup landed in the meantime), the request is refused so the admin re-previews. 12 new tests cover validation + drift + happy path + notifications-insert failure mode. Web: `useAdminAudiencePreview` (30s stale, refires on audience change) + `useAdminSendCampaignPush` hooks; `<CampaignsPage>` with two-pane layout (composer on left, live audience preview card on right showing count + first-10 sample emails); confirm-send dialog echoes count + title + body + reason, requires re-typing the recipient count for ≥100 recipients. Sidebar's "Campaigns" link now resolves to the real page instead of the Placeholder.
- [x] **1.5** Email broadcast (Resend + TipTap rich-text editor) — shipped 2026-05-17. Migration 080 extends `campaigns` with `channel TEXT NOT NULL DEFAULT 'push'` (CHECK push|email) + `email_from TEXT`. Existing rows backfill to push; row reuses `title` (=subject) + `body` (=HTML) + `recipient_count` + `push_sent_count` (=delivered count, channel-agnostic). New `server/lib/resend.ts` thin wrapper (singleton client, hard-fails the send path if RESEND_API_KEY missing, batch fan-out with concurrency=10). Allowlist of 4 From addresses (`marketing@`, `hello@`, `support@`, `admin@` @ tagorides.com) enforced server-side. New `GET /api/admin/campaigns/from-addresses` returns the allowlist; new `POST /api/admin/campaigns/email` body `{ audience, subject, body_html, from, reason?, confirm_count?, test_to_self? }` validates → resolves audience → filters by `email_marketing` opt-outs (vs push_promos) → creates campaign row with channel='email' → looks up recipient emails → fans send via Resend → updates delivered count → audits with action='send_campaign_email'. Audience preview endpoint accepts `?channel=email` and switches opt-out column. 6 new tests cover body validation + From allowlist + email_marketing filter + test_to_self + happy path. Web: new dep `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link` + `@tiptap/extension-placeholder`. `<RichTextEditor>` wrapper with toolbar (bold/italic/strike/H1/H2/lists/blockquote/link/undo/redo). CampaignsPage gets a Push|Email tab switcher above the composer; email-mode swaps in From dropdown + subject input + RichTextEditor. Confirm-send dialog renders email body via `dangerouslySetInnerHTML` for accurate preview. History list shows a green `email` / blue `push` chip per row + Duplicate switches to the right tab. 1216/1216 tests.
- [~] **1.6** In-app banner integration (web + iOS) — **web half shipped 2026-05-17.** `<ForegroundPushToast>` (mounted once at AuthGuard) gains a first-class `admin_broadcast` branch: renders 56×56 poster thumbnail (when `data.image` present) alongside title + body in a primary-tint card, dwells 10s instead of the 6s default (marketing copy needs more time to absorb), and taps route to `/c/<slug>` (same destination as the OS-banner notificationclick handler in firebase-messaging-sw.js) — falls back to `/notifications` when the slug is missing. Until this slice, foreground tabs got a generic neutral toast via the `default` branch (no poster, wrong tap target, short dwell). 7 new tests (1223/1223 total): poster renders / poster absent / slug tap / no-slug fallback / 10s dwell holds past 6s / pre-existing types still work / pre-existing HANDLED_ELSEWHERE filter still drops ride_request. **iOS half deferred** (not in this slice): when the iOS app is foregrounded the OS shows the native banner; the iOS-side parity work is to suppress the OS banner and present an in-app one via `UNUserNotificationCenterDelegate.willPresent` returning `.banner` selectively + a tiny BannerPresenter view. Parked for the next iOS-touching session.
- [x] **1.7** Live ops view (map + event feed) — shipped 2026-05-17. Server: new `GET /api/admin/live/snapshot` returns active rides (status in accepted/coordinating/active, capped at 200) + last-60-min event feed (created/started/completed/cancelled, capped at 100) + per-user name lookup (full_name fallback to email local-part). Truncation flags fire when caps are hit. 7 new tests cover permission gate + empty / single-ride shape / event synthesis from one row / cancelled-vs-completed branch / 200-cap truncation. Web: `useAdminLive` React Query hook polls every 10s (paused when tab loses focus via `refetchIntervalInBackground: false`); `<LiveOpsPage>` at `/admin/live` with 3 sections — Google Map (2/3 width, primary-dot driver pin + warning-square pickup pin per ride, legend overlay), event feed (1/3 width, scrollable, color-coded kind dots, time-ago stamps), active-rides table beneath. New `Live` route in `main.tsx` (sidebar item was already present). No new migrations.
- [x] **1.7d-fix** Online-gate alignment with matcher — shipped 2026-05-17. Caught by Tarun: `/admin/live` showed 0 online drivers despite testdriver having `is_online=true` + a 6m45s-old ping + a valid push token. Original 1.7d gate used a 5-min ping freshness window which hid drivers from admin even though the matcher Stage 1 fallback would still notify them (Stage 1 has no freshness gate — the 2026-05-04 fix in `rides.ts` removed it because iOS delivers visible alert pushes to force-quit apps whose `recorded_at` can't refresh). Fix: change `ONLINE_PING_WINDOW_MIN = 5` → `ONLINE_INCLUSION_WINDOW_HOURS = 24 * 7`, matching `clearStaleOnlineFlags` (which is the actual "this driver no longer counts" boundary). Added `ping_stale: boolean` + `ping_age_ms: number` to `OnlineDriver`; UI surfaces `ping_stale` as a dimmer dot + warning border on the map, "N stale ping" in the KPI sub-line, and an explanatory tooltip ("matcher still pushes"). Map legend gains a "Online · stale ping" row. 1 new regression test ensures a driver with a 6-min-old ping appears in `online_drivers` with `ping_stale=true`. No migration.
- [x] **1.7f** Manual driver-to-ride reassignment — shipped 2026-05-17. Ops escape hatch when the matcher hasn't found a driver or when ops knows a specific driver should take this ride. New `POST /api/admin/rides/:rideId/reassign` body `{ new_driver_id, reason }`. Validation: UUID format, ride exists, ride is requested/accepted (mid-flight reassign out of scope — would need re-routing), new driver exists + is_driver + not suspended + not on another in-flight ride. Write: `ride.driver_id = new_driver_id`, status flips 'requested'→'accepted' (otherwise unchanged), 2–3 inbox notifications (new driver / rider / previous driver if any), 2–3 audit rows (one per role), best-effort FCM push fan-out. Does NOT touch the matcher RPC or any staged matching logic — pure DB write + notify, respecting CLAUDE.md's staged-matching rules. Web: `<ReassignCard>` in the LiveOps drawer with driver picker (populated from `online_drivers` minus current driver minus busy drivers), reason textarea, two-step confirm. Card gracefully degrades to an info line when ride status is non-eligible. 13 new server tests cover every validation + happy path with/without previous driver. 1284/1284 total. No migration.
- [x] **1.8b** Audit log free-text search — shipped 2026-05-17. New `q=` query param on `GET /api/admin/audit-log` runs `query.or('action.ilike.%q%,payload::text.ilike.%q%')` — single round trip, hits the existing newest-first index, no migration. Server strips PostgREST-breaking chars (`, ( ) *`) so a user-typed `q` can't escape the OR clause. Web: `q` added to `AuditLogFilters` + a "Search" input added to the filters card (positioned first since it's the most-used filter). 2 new server tests cover the .or() clause shape + char-sanitization. Most common use case: ops searches for "spring fling" (the campaign title) or "$50" (a refund amount) to find a specific incident across all admin actions.
- [x] **1.7e** Live ops free-text search — shipped 2026-05-17. Client-side filter input above the active-rides table (paired with the existing filter pills in the same flex row). Matches the search string against rider_name / driver_name / origin_name / destination_name (case-insensitive). Pure client-side because the snapshot already carries names. No new tests needed — purely additive UI on data the existing tests already validate.
- [x] **1.7d** Online drivers overlay + Live KPI strip — shipped 2026-05-17. Extends `/api/admin/live/snapshot` with `online_drivers: OnlineDriver[]` + `available_driver_count: number` + `snoozed_driver_count: number` + `online_drivers_truncated: boolean`. "Online" reuses the matcher RPC's gate (`driver_locations.is_online=true + recorded_at within 5 min + snoozed_until null OR past + user is_driver + user not suspended`) so the admin sees the same supply pool the matcher does. Each driver row carries `on_active_ride` so the UI can tell idle from busy. Capped at 500 with truncation flag. Web: 4-card KPI strip above the filter pills (Active rides / Stuck / Online drivers / Snoozed, each with an InfoTooltip explaining the source). Map renders idle drivers as green-pulse dots (busy drivers excluded — they're already drawn via the active-ride primary-dot to avoid double markers); top-right toggle hides/shows them; legend updates to include "Online driver (idle)". 2 new server tests cover the gate (snoozed/suspended/non-driver exclusion) + the on_active_ride cross-reference. 1269/1269 total. No migration — pure query against existing tables.
- [x] **1.7c** Stuck-step audiences in /admin/campaigns — shipped 2026-05-17. 4 new `AudienceType` values that target users whose furthest funnel step is exactly the named step (they reached that step but never progressed to the next): `stuck_after_signup` / `stuck_after_verified_email` / `stuck_after_completed_profile` / `stuck_after_payment_or_vehicle`. Server: `resolveAudience()` dispatches stuck audiences to a new helper that calls the exported `computeFunnelData('all', 'both')` from funnel.ts and filters cohort by `maxStep === stepIdx`, then applies the existing suspended + opt-out filters. `parseAudienceFromQuery` + `parseAudienceFromBody` validTypes both extended. Web: 4 new options added to `AUDIENCE_OPTIONS` in CampaignsPage with marketing-ready labels and descriptions; works seamlessly on both Push + Email tabs (resolution is channel-agnostic, opt-out filter applies the right column per channel). 5 new server tests cover each stuck type + suspended-user exclusion. **No migration** — pure logic add reusing existing data. Most common ops use case: rider stuck at payment_or_vehicle → "your first Tago ride is free, finish setup" nudge.
- [x] **1.7b** Live ops micro-management — shipped 2026-05-17. Extends `/api/admin/live/snapshot` to include `requested` status rides (no-driver-yet visibility) and computes `stuck_reason` server-side per row: `awaiting_driver` (requested >5m), `coordinating_long` (coordinating >5m), `driver_gps_stale` (active + last driver ping >2m old), `ride_long` (active + started >30m). Thresholds exported so server tests assert against the same constants. New `POST /api/admin/rides/:rideId/cancel` endpoint: refuses if ride is terminal (409), otherwise flips `status='cancelled' + ended_at=now()`, inserts notifications row per party + best-effort FCM push to each (type='admin_cancel'), audit-logs once per party (mirrors refund's two-row pattern). Does NOT touch payment_status — refund is a separate decision. Web LiveOpsPage gains: filter pills (All / Stuck only / Awaiting driver / Accepted / Coordinating / Active, each with a count badge); stuck-ride red tint on table rows + danger-color map dot; row + marker click opens a right-side drawer with ride detail + 3 action sections — Profile shortcuts (deep-link to rider / driver `/admin/users/<id>`), Quick-push (mini composer per party with prefilled "checking in on your ride" copy, reuses `useAdminSendPush`), Force-cancel (reason-gated confirm flow with destructive-tone styling). 10 new endpoint tests cover stuck detection variants + truncation; 7 new tests cover force-cancel permission/validation/happy paths. 1240/1240 total. No new migrations.
- [x] **1.8** Campaign history + audit log — shipped 2026-05-17. Campaign history already shipped as part of 1.4d (history panel on /admin/campaigns with recall + duplicate). This slice adds the global audit log page. Server: new `GET /api/admin/audit-log?limit=&offset=&action=&admin_id=&target_user_id=` returns every row from `admin_audit_log` paginated newest-first, hydrates admin_email + target_email in one round trip (single users `.in()` lookup with deduped ids), supports the magic value `target_user_id=null` to filter broadcast actions (campaigns) with no target. Returns `distinct_actions` for the current page so the client's filter dropdown populates without a second round trip. UUID filters validated server-side before hitting Postgres (malformed UUID returns 400 INVALID_*). Limit capped at 100. 10 new tests cover permission gate + hydration + broadcast null target + distinct_actions list + UUID validation + limit cap + filter pass-through. Web: `useAdminAuditLog` React Query hook with `keepPreviousData` so pagination doesn't blank during refetch; `<AuditLogPage>` mounted at `/admin/audit-log` with a filters card (action dropdown + 2 UUID text inputs + "Clear all"), a table with When / Action chip / Admin email + UUID / Target with deep-link / collapsible JSON payload viewer (with one-line summary line that adapts per action shape), prev/next pagination. No new migration — uses migration 074's existing indexes.
- [x] **1.9** Operational alerts panel — shipped 2026-05-17. Single endpoint `GET /api/admin/alerts` returns 5 categories of "needs ops attention" signal in one round trip: (1) **stuck_rides** — reuses Slice 1.7b's computeStuck thresholds (5m/5m/2m/30m), worst-first sort; (2) **failed_payments** — rides with payment_status='failed' from Stripe webhook; (3) **unpaid_completed** — completed rides with payment_status not in ('paid','refunded') older than 10 min (webhook-drift signal, bounded to last 7 days); (4) **negative_wallets** — users with wallet_balance < 0 (typical cause: refund overdraft from Slice 1.3e); (5) **recent_suspensions** — last 24h, FYI only. Each category caps at 50 items with a truncated flag. Name hydration (rider_name / driver_name / name) done in a single deduped `.in()` lookup. 11 new server tests cover permission gate + empty state + stuck classification (3 cases incl. worst-first ordering) + payment categories + negative wallets + suspensions + total_count sum. Web: `useAdminAlerts` React Query hook polls every 30s (paused when tab loses focus); `<AlertsPage>` at `/admin/alerts` with 5 SectionCards each tinted by tone (danger/warning/neutral) — empty categories collapse to a green "All clear here" message so the page stays scannable; row click-through deep-links to user profile / Live ops drawer. New `Alerts` item in the sidebar with a live red `total_count` badge driven by the SAME `useAdminAlerts` call used on the page — admins see urgency without opening the page. No new migration.
- [ ] **1.10** Final QA + prod ship

## Phase 2 — Power features (post-Phase-1)

- [ ] **2.1** Custom audience builder (drag-drop query UI)
- [ ] **2.2** A/B testing for campaigns
- [ ] **2.3** Drip campaigns (event-triggered)
- [ ] **2.4** User cohort analysis (retention heatmaps)
- [ ] **2.5** Ride analytics deep dives (heatmap, wait times, cancellation reasons)
- [ ] **2.6** Financial reports (revenue, fees, Stripe Connect, refunds, CSV export)
- [ ] **2.7** Feature flags (per-user / segment / global)
- [ ] **2.8** Content management (FAQ, safety alerts, announcement banners)
- [ ] **2.9** Bulk operations (refund / credit / import via CSV)
- [ ] **2.10** Slack/Discord integration (real-time alerts)

## Phase 3 — Future

- [ ] **3.1** Role-based access control (admin / marketing / ops / finance / support)
- [ ] **3.2** SSO + 2FA for admins
- [ ] **3.3** Granular audit + compliance (SOC 2 prep)
- [ ] **3.4** Mobile admin app (iOS)
- [ ] **3.5** Data warehouse export
- [ ] **3.6** Multi-tenant admin (if Tago white-labels)

---

## Decisions log

Append-only. Newest entries at the top. Capture every non-obvious decision so future-you (or a teammate) doesn't have to re-derive them.

- **2026-05-17** — **Slice 1.7d-fix: admin "online" definition must match the matcher, not be stricter than it.** Original 1.7d used a 5-min ping freshness gate borrowed from the `nearby_active_drivers` RPC (Stage 2 matcher). That was wrong — Stage 2 only runs when nearby drivers exist; the Stage 1 fallback (which handles every other case) has NO freshness gate per the 2026-05-04 rides.ts fix. Tarun's design (preserved in `clearStaleOnlineFlags`'s `STALE_ONLINE_HOURS = 24 * 7`) is that a driver who toggled is_online=true stays online for **7 days** of no pings — the visible alert push reaches force-quit iOS apps that can't refresh their ping. So `/admin/live` must use the 7-day window too, otherwise admin says "0 online" while the matcher is happily pushing to drivers. The 5-min threshold lives on as a UI warning (`ping_stale=true`) rather than a hard filter. Rule: any admin "is this online?" check should reuse the matcher's actual gate, never a tighter one.
- **2026-05-17** — **Slice 1.7f reassign rejects mid-flight rides + double-booking.** Allowing reassign on `coordinating`/`active` rides would mean handing a partially-completed pickup to a new driver — the rider may have already met the original driver, GPS state is invalid, the new driver has no context for the rendezvous point. Out of scope for v1; force-cancel + new ride request is the right ops path for that case. Double-booking guard prevents the new driver from being on two rides at once (matcher would surface the conflict eventually, but better to fail-fast at write time with a clear 409 than ship the user a confusing "your driver is already busy" UX). Both gates server-enforced + tested.
- **2026-05-17** — **Slice 1.7f writes notifications + audit per party, mirroring refund's two-row pattern.** Refund writes one audit row per party (rider + driver) so both users' Admin Actions audit lists pick it up. Reassign extends to three parties (new driver + rider + previous driver if any), each getting their own audit row tagged with `role`. Trade-off: 2–3 audit writes per reassign instead of 1 row keyed off the ride. Cost negligible; per-user discoverability is the win.
- **2026-05-17** — **Slice 1.7d "online" definition mirrors the matcher RPC exactly.** Considered defining "online" loosely (any driver with `last_active_at` recent, any driver with a push token). Both would over-count — riders who happen to use the app would show as drivers; drivers who'd toggled themselves offline would still appear. The matcher RPC in migration 064 already encodes "is this driver receiving the next ride notification?": `driver_locations.is_online + recent ping + not snoozed + is_driver + not suspended`. Reusing that gate guarantees the admin number IS the matcher's pool — no surprise gaps between "the dashboard says 5 drivers online" and "we sent the push to 5 drivers." If a driver doesn't have a push token row they still appear on the map (their app is open), but the matcher would skip them; that's a known cosmetic over-count we can fix in a follow-up by joining `push_tokens`.
- **2026-05-17** — **Slice 1.7d busy drivers excluded from the dots overlay.** A driver currently driving a ride appears already as the active-ride driver dot (primary color, with the ping animation). Drawing them ALSO as a green online-driver dot at the same coords would double-render the marker and confuse "supply at a glance." Filtered them out at the render boundary instead of the query — the server still returns `on_active_ride: true` so the future driver-detail drawer can show their state correctly, and the available count is computed before the filter.
- **2026-05-17** — **Slice 1.9 unpaid_completed cutoff = 10 min, bounded to last 7 days.** Stripe charge webhooks normally arrive within seconds. A 10-minute grace window catches the genuine misses without false-positiving rides that are mid-flight. Bounded the lookback to 7 days because (a) anything older is either resolved manually or a write-off — surfacing it daily is noise, (b) keeps the query indexed-friendly (`ended_at` range scan + `status` + `payment_status` is cheap when bounded). Trigger to widen: if ops finds an unpaid ride older than 7 days that wasn't surfaced.
- **2026-05-17** — **Slice 1.9 stuck-ride detector reuses live thresholds — single source of truth.** Considered duplicating the stuck logic with different (higher) thresholds for alerts ("only fire if stuck for >10 min, not 5"). Dropped: one threshold + one detector keeps the count between /admin/live's filter pill and /admin/alerts in sync. If 1.9 needs different semantics later, refactor the threshold to a config param rather than fork the logic.
- **2026-05-17** — **Slice 1.9 polls at 30s, not 10s like Live ops.** Live ops needs sub-15s freshness (ops actively driving rides on the screen). Alerts is more like an email inbox — ops glances at it periodically. 30s halves the request volume of /live and matches the dwell pattern: open Alerts → scan → fix one → move on. The sidebar badge is the always-on signal that drives WHEN to open Alerts; the page itself is the workspace.
- **2026-05-17** — **Slice 1.9 sidebar badge reuses the same React Query call as the page.** Considered firing a separate "count only" endpoint to keep AdminLayout light. Dropped: same query key + 30s stale time means whichever surface mounts first warms the cache for the other. Total cost is one round trip per 30s regardless of how many places read it.
- **2026-05-17** — **Slice 1.8 audit-log filters limited to action + admin + target.** Considered adding free-text payload search + date range pickers + action multi-select. Dropped all three for v1: payload search needs a tsvector index or jsonb path query (extra migration), date range adds two more inputs that 90% of investigations don't need (newest-first list + pagination is enough for "show me today"), and action multi-select doubles the dropdown UX (single dropdown + use the "filter by admin who fired it" approach for cross-action investigations). If a real investigation pattern emerges that needs any of these, add it then.
- **2026-05-17** — **Slice 1.8 distinct_actions baked into the response, not a separate endpoint.** Considered exposing `GET /api/admin/audit-log/actions` for the dropdown. Dropped: distinct list from the current page is good enough for the common case (recent actions show first), one round trip is faster than two, and a global distinct list would have to scan the entire table — wasted work when the dropdown's job is just to populate the filter chooser. Edge case: an action type that hasn't been used recently won't appear in the dropdown until the user pages back to find it. Trigger to fix: ops complaint or > 50 distinct action types.
- **2026-05-17** — **Funnel completed_first_ride bug fix: completed-ride wins over cached payment column.** Original `computeFunnelData` walked steps in strict order: payment_or_vehicle had to pass before completed_first_ride was even checked. That clamped riders who paid entirely from wallet balance (promo credit / refunds / top-ups) — they never saved a `default_payment_method_id` so the funnel said they were stuck at "completed_profile" even after finishing rides. Tarun caught this when his test rider showed in the stuck audience despite completed rides. Fix: a completed ride lifts the user to `max=4` regardless of the cached payment column. Rationale: actually completing a ride is empirical proof the platform's pre-ride gates let them through (server/routes/rides.ts has a wallet-only path at line 419 — `walletCoversEstimate` lets a request through without a saved card). Trade-off: payment_or_vehicle step COUNT now under-reports users who paid by wallet (they'd never satisfy step 3 by the cached column), but those users now correctly appear at step 4 instead of being miscategorized. New regression test pins the wallet-only case (1246/1246). Same fix benefits the 4 stuck-step audience types from Slice 1.7c — wallet-only riders no longer get nuisance-targeted by "finish setting up" emails.
- **2026-05-17** — **Slice 1.7c reuses computeFunnelData rather than duplicating membership rules.** Considered writing a separate "is stuck at X" SQL query for each new audience type. Dropped: would mean two definitions of "stuck at verified_email" — one in funnel.ts (chart), one in campaigns.ts (audience). They'd inevitably drift. Exporting `computeFunnelData` is one extra import and one shared source of truth — when the funnel chart says "14 users stuck at verified_email" the audience preview WILL say 14 too (modulo opt-outs). Trade-off: every stuck-audience preview triggers the full funnel computation (users + auth.admin.listUsers + vehicles + rides), which is heavier than a flat users query. Acceptable while user counts are small; the existing 30-second client cache + the 2-3 second preview round trip masks the cost.
- **2026-05-17** — **Slice 1.7c stuck audiences run all-time + both roles, not the funnel chart's range/mode.** Funnel chart filters by `range=7d/30d/90d/all` and `mode=rider/driver/both` so ops can see retention movement over windows. Stuck-AUDIENCE has no such control — marketing wants to reach EVERY user who's been parked at a step, not just those who signed up in the last 30 days (older parked users are the more valuable nudge target). Hardcoded `('all', 'both')` rather than expose two more pickers in the campaigns composer; if a niche filter (e.g. "stuck riders only") becomes a request, we can layer a sub-filter UI later.
- **2026-05-17** — **Slice 1.7b force-cancel does not refund — explicit two-step.** Considered making force-cancel automatically refund any paid fare. Dropped: an `active` ride that the rider partially completed before the driver vanished may legitimately still owe SOMETHING; an admin needs to look at the ride length / GPS trail before deciding. Auto-refund would also overwrite the existing refund's careful driver-overdraft handling. The drawer instead surfaces a "deep-link to rider profile" button so the admin lands one click from the Rides tab where the refund button already lives. Trade-off: two clicks for the common case (cancel + refund). Acceptable because the cancel-without-refund case (no-show, rider unreachable, never started) is just as common — bundling would force admins to opt OUT every time.
- **2026-05-17** — **Slice 1.7b stuck thresholds locked at 5m/5m/2m/30m.** Drew from typical Tago ride patterns: most pickups complete within 4 min of acceptance (so a 5m awaiting_driver flag catches the genuine misses without firing on the bulk of normal traffic), coordination averages 2-3 min (5m is the long tail), driver GPS pings normally arrive every 10-15s (2m without one means the app was killed or signal lost), typical ride duration 8-12 min (30m is 2-3x normal, almost always a forgotten-end-button case). All exported as constants from `server/routes/admin/live.ts` so tests + future config can rely on the same numbers. Trigger to revisit: ops feedback that thresholds fire too often or too rarely.
- **2026-05-17** — **Slice 1.7b drawer is a fixed-right panel, not a modal.** Considered `<BottomSheet>` (matches consumer-app pattern) and a modal dialog (matches admin pattern elsewhere). Picked fixed-right: ops needs the map and event feed STILL visible while deciding what to do — a modal would dim them, a bottom sheet would cover them. The drawer takes 1/3 of the screen on a 1080p monitor, leaving the map fully interactive. Closing with the X button is the dismissal — no backdrop click since the surrounding map needs to keep accepting drags.
- **2026-05-17** — **Slice 1.7 picks 10s polling over Supabase Realtime.** Considered subscribing to `rides` table changes via supabase-js channels for sub-second freshness. Dropped because (a) the admin Live tab is rarely the foreground tab — most ops users glance at it, then go back to work; sub-second is overkill and burns more connections than it's worth; (b) a single endpoint poll keeps the data shape stable across requests (timestamps + cap flags); a multi-row subscription would have to do its own incremental aggregation on the client; (c) React Query already pauses polling when the tab loses focus, so we don't waste server cycles on a hidden tab. Trigger to revisit: ops needs sub-second to chase a real-time incident, OR the cap of 200 active rides is hit and we need to filter server-side. Realtime layered on the same `/snapshot` shape is easy when the time comes.
- **2026-05-17** — **Slice 1.7 events table = rides table only; notifications + payments not part of v1.** Considered widening "events" to include `notifications.created_at` (new pushes), `transactions.created_at` (wallet ops), and `audit_log.created_at` (admin actions). Dropped for v1: each adds another query + name lookup + UX decision (how to colour-code 4 event sources without the feed turning into a Christmas tree). Ride lifecycle alone is the most operationally relevant — that's what ops investigates ("why is this ride stuck?") and what the map needs for context. Other event sources can be opt-in toggles in Phase 2 Slice 2.5 (ride analytics deep dives) or fold into Slice 1.9 (operational alerts panel).
- **2026-05-17** — **Slice 1.7 active-rides table sits below the map, not in a tab.** Considered tabs (map | table | timeline). Dropped: ops needs the table AND the map AND the feed at-a-glance — tabs would force a click each time. 640px-tall map + feed + scrollable table fits on a 1080p monitor without competing for vertical space. The table also serves as fallback content when the Google Maps key is missing or fails to load.
- **2026-05-17** — **Slice 1.6 web half lands without iOS half — split intentional.** Foreground browser tabs were the most broken case: the OS / browser actively suppresses native push banners for focused tabs, so dropping the message was silent and routed via the generic-default branch (no poster, ride-board fallback). Web half is also self-contained and adds zero deploy/build risk to the iOS app. iOS foreground behaviour is materially different — the OS already presents a banner-style notification when the app is foregrounded (unless `willPresent` returns `[]`), so the parity work is mostly cosmetic (custom in-app banner with Tago tint instead of OS default). Bundling iOS would have meant another build + device install + Console.app debug loop in the same slice without unlocking new user value. Ship the broken case now.
- **2026-05-17** — **Admin broadcast banner dwell = 10 s, not 6 s.** Default ForegroundPushToast dwell is 6s, which suits actionable system events (payment_received, schedule_match) where the user knows what to do. Admin marketing carries unfamiliar copy + a poster that takes a beat to register. Picked 10s as the round-up of "enough to read a 1-sentence headline + skim the body + glance at the poster without feeling stuck." Long enough to not flash-by; short enough that an admin who set up an unfortunate banner doesn't camp on every user's screen. Override via per-toast `durationMs` rather than a config flag so future high-stakes events (e.g. emergency alerts in Phase 2.8) can opt up without a refactor.
- **2026-05-17** — **Slice 1.3c split into 1.3c (3 simple actions) + 1.3d (3 high-risk actions).** Original 1.3c scope was all 6 write surfaces + audit log. Chose to ship Send-push + Grant-credit + Override-onboarding now because (a) audit log + 3 actions is already substantial, (b) the deferred 3 each touch a different shared system that needs its own design pass: Suspend requires a sign-in gate plus a new `suspended_at` column AND coordination with iOS/web client gates; Refund touches `wallet_apply_delta` for both rider and driver plus has to handle ride-status transitions; Force-reset is lowest-priority because Supabase already exposes a "Forgot password?" link on sign-in. Splitting lets ops use the most-common actions today without waiting on the cross-system pieces.
- **2026-05-17** — **Slice 1.3c audit failure swallowed (logged not thrown).** `writeAuditLog()` warns + returns when the insert fails instead of throwing. Reason: the action it follows (e.g. push send, wallet credit) ALREADY succeeded by the time we audit-log. Failing the request because the audit row didn't land would leave the admin with a confusing 500 after their action actually happened. Better to surface the audit miss in server logs + ops alerts (Slice 1.9) and keep the user-facing flow snappy. Acceptable because every audit failure leaves a `[adminAudit]` log line we can grep.
- **2026-05-17** — **Slice 1.4 picks 7 audience filters, defers custom builder to Phase 2.** Considered (a) shipping all_users only and letting custom queries land in Phase 2 (too narrow — ops needs at least drivers/riders/by-university today); (b) starting with a drag-drop query UI like Mixpanel (too heavy for first cut — would double the slice). Picked 7 pre-baked filters that cover the common cases: role split (driver/rider/all), university targeting (most-frequent marketing case), and activity-based re-engagement (active recently / dormant). Custom audience builder is locked in for Phase 2 Slice 2.1.
- **2026-05-17** — **Slice 1.4 confirm-count drift check uses the value the preview returned, not a server-side timestamp.** Considered storing the preview's resolved user_ids in a short-TTL cache and replaying them at send time. Decided against: (a) requires a new table or Redis, (b) the audience the admin SAW (count) and what gets sent should match by INTENT — a slightly stale count is the user-visible signal, not the specific user_ids. If 99 → 100 between preview and send, send proceeds; if 99 → 150, the admin should re-preview to confirm.
- **2026-05-17** — **Slice 1.4 writes notifications rows for every recipient even if push send failed for that user.** Reason: the in-app inbox is its own surface (NotificationsPage on web + iOS). A failed push (e.g. user has no token registered) shouldn't mean they miss the message — they'll see it next time they open the app. Notifications insert is best-effort; if THAT fails (very rare), audit row still writes + push still sent, just the inbox is missing. Logged loud.
- **2026-05-17** — **Slice 1.3e refund writes 2 audit rows, one per party.** Rationale: the per-user audit list on the user-detail Admin Actions tab is keyed on `target_user_id`. A refund affects BOTH the rider AND the driver — writing a single row keyed on either would hide the action from the other party's audit history. Two rows cost ~80 bytes of storage each and surface the refund on both parties' history naturally. Each row carries the role + amount sign so the summary line reads correctly ("rider +$5.00" vs "driver −$5.00").
- **2026-05-17** — **Slice 1.3e atomicity is best-effort, not transactional.** Refund makes two sequential `wallet_apply_delta` calls (rider credit, then driver debit). If the second fails after the first succeeded, Tago is out by `fare_cents` and the rider has the money. The endpoint logs CRITICAL + returns 500 DRIVER_DEBIT_FAILED with the rider's new balance so manual reconciliation has the context. We mirror the same split-write pattern Tago uses for ride payment in `server/routes/rides.ts` rather than introduce a new transactional layer just for the refund path. If reconciliation pain ever surfaces, the right fix is a SQL `wallet_apply_delta_pair` stored procedure (out of scope today).
- **2026-05-17** — **Slice 1.3e refund UI sits on the Rides tab, not Admin Actions.** Two surfaces would also work, but Rides-tab placement is most discoverable: admins already look up someone's ride history when investigating a complaint. A per-row Refund button + confirm modal completes the workflow without leaving the page. The Admin Actions tab stays focused on user-level operations (push, credit, suspend, etc.).
- **2026-05-17** — **Slice 1.3d skips suspension gate under VITEST.** validateJwt's new suspended-user gate does a DB lookup on every authenticated request (cached 60s/user). Tests use `mockFrom.mockReturnValueOnce` queues that expect a specific call order — adding an extra `from('users')` call here shifted every queue by one and broke ~80 unrelated tests. Gated the lookup on `process.env.VITEST === 'true'`. Real production behaviour is unaffected; the suspension gate's correctness is tested directly via the action-endpoint tests (admin flips column, verifies endpoint behaviour). The trade-off: the validateJwt gate itself isn't unit-tested in isolation — a follow-up test would need to override the VITEST gate OR set up a more elaborate mock harness.
- **2026-05-17** — **Slice 1.3d suspension cache invalidation is push, not pull.** Status cache is 60s TTL per user. When an admin suspends/un-suspends, the action handler calls `invalidateUserStatusCache(id)` so the next request sees the change immediately. Without this, a freshly-suspended user could keep using the app for up to 60s. PM2 single-process scope means the cache is per-instance; restarts re-arm it (one extra DB read per user post-restart — acceptable).
- **2026-05-17** — **Slice 1.3d Refund split to 1.3e.** Refund needs (a) a per-ride entry point — natural fit is a button per row in the Rides tab, not the Admin Actions tab; (b) driver-overdraft handling — refunding a ride where the driver has already withdrawn the wallet credit means the driver's balance would go negative, requiring explicit admin confirmation; (c) potentially Stripe API calls if the rider's original payment hit Stripe directly (vs internal wallet). Each of those is its own design decision; bundling with the simple Suspend + Force-reset would've doubled the slice size.
- **2026-05-17** — **Grant-Credit gets two-layer safety: Stripe balance hard-block + confirm dialog with re-type for ≥$50.** Triggered by Tarun's question on first-look: "what if Stripe doesn't have enough, and what if I mis-click?" Server-side gate uses Stripe's *available* balance (not pending — pending funds aren't transferable). Outage = block, not allow ("err on the side of safety"). Web confirm dialog echoes amount + email + reason; re-type is gated on the $50 threshold rather than always-on to keep small ops grants snappy. Skipped admin-password re-entry: marginal value over the dialog given the session is already authenticated, and would feel like ceremony for a 1–3 person team sharing one account. Will revisit if multi-person admin access lands or compliance pressure shows up.
- **2026-05-17** — **Slice 1.3c admin push send bypasses notification preferences.** The user-facing `/api/notifications/send` (server/routes/notifications.ts) enforces a same-ride gate AND respects per-user notification preferences. The admin push endpoint deliberately reuses neither: it queries `push_tokens` directly and calls `sendFcmPush` raw. Reasoning: admin push is a manual operator tool, not a behavioral notification — preference-checking would silently drop pushes ops just told us they want sent. Audit log records the title + body + counts so the operator-side accountability is intact.
- **2026-05-17** — **Slice 1.3 split into 1.3a / 1.3b / 1.3c.** Original spec was one mega-slice (6 tabs + audit log + 6 write surfaces). Chose to ship search + Overview tab first (1.3a) because that alone unblocks marketing's "find any user in seconds" use case. 1.3b will add the four other read-only tabs (Rides, Wallet, Notifications, Devices) in one sweep. 1.3c will tackle the highest-risk piece: Admin Actions write surfaces + the audit_log infra. Splitting lets us ship 1.3a value tonight without committing to building audit infra in the same chunk.
- **2026-05-17** — **Slice 1.3a search: full UUID = exact match, partial UUID prefix deferred.** PostgREST doesn't let us cast `id::text ILIKE` in ad-hoc filters and we didn't want to spawn a SQL function just for prefix search. Full UUIDs (copy-pasted from logs / admin actions) get an exact-id lookup; partial UUID prefixes return no match. If marketing needs partial UUID, the fix is a small SQL RPC `admin_user_search(q text)` that joins `id::text ILIKE` with the existing text fields — fold into a follow-up slice if it comes up.
- **2026-05-17** — **Slice 1.3a universityFromEmail is a hardcoded switch, not a config.** Considered loading a curated map from a JSON file or DB table. Hardcoded the switch in server/routes/admin/users.ts because there's exactly one consumer (the admin Overview tab) and the list of UC campuses + Stanford + MIT + Harvard + tagorides.com is unlikely to churn. To add a school: drop a line in the switch.
- **2026-05-17** — **Slice 1.2 funnel step 4 semantics for mode=both.** Step 4 = "Added payment method (rider) OR registered vehicle (driver)". Considered (a) splitting into two parallel sub-steps, (b) requiring both. Chose role-aware single step: a rider satisfies it via `default_payment_method_id`, a driver via a non-deleted vehicle. Means the same person's "ready to participate" milestone gets evaluated against their actual role, not against a generic union. Mode=rider/driver filters before this decision so the column always means one specific thing per query.
- **2026-05-17** — **Slice 1.2 email verification via `auth.admin.listUsers`, not a join.** supabase-js doesn't let us SELECT from the `auth` schema directly. Two options: write a SQL function that joins public.users to auth.users (clean but requires migration 072 just for this), or paginate `auth.admin.listUsers()` and merge in TS (works today, no migration). Picked TS for the same reason as Slice 1.1: dataset is small and we don't want to spawn migrations for every analytics endpoint. Will switch to a SQL join when user count > 10k or when latency complains.
- **2026-05-17** — **Slice 1.2 mode=both: don't filter cohort by is_driver, but still role-aware for steps 4 & 5.** A rider-as-driver who's flipped is_driver after onboarding shouldn't show up "stuck at vehicle" if they were never going to drive. The mode=both view evaluates step 4 against whichever role is_driver says they are.
- **2026-05-17** — **Slice 1.1 grid layout: 12 cards in 4×3, DAU/WAU/MAU combined.** Original spec listed 14 metrics but capped at "12 cards in a 4×3 grid". Resolved by collapsing DAU/WAU/MAU into one compound "Active users" card showing all three numbers stacked — matches how Mixpanel / Amplitude present this triad. Trade-off: slightly more dense card; one less drill-down target if/when we add click-through.
- **2026-05-17** — **DAU/WAU/MAU data source = new `users.last_active_at` column + middleware bump.** Considered (1) ride-activity proxy, (2) defer KPI. Chose the column because Tago needs a real DAU metric long-term and adding it now means the dashboard's number is honest from week one (only retroactive limitation: pre-migration users start at NULL, backfill as they next hit the API). Bump lives in `validateJwt` (in-memory throttle, 5 min) so chatty clients don't hammer the table. Single-process PM2 means the throttle is per-instance; a restart re-arms it for one extra write per user, which is fine.
- **2026-05-17** — **iOS install rate data source = new `push_tokens.platform` column.** Considered (1) drop KPI and replace with "users with push enabled", (2) defer entirely with a placeholder card. Chose the column because the call site (web `requestAndSaveFcmToken` / iOS FCM bridge) trivially knows the platform — writing it is free and the KPI becomes meaningful as tokens refresh. Web client writes `'web'` in this slice; iOS client will write `'ios'` in the next iOS-touching slice (rebuild + device install per protocol — kept out of Slice 1.1 to avoid bundling unrelated iOS rebuild risk). Until that ships, all existing iOS users count as NULL = unknown, so iOS install rate looks artificially low.
- **2026-05-17** — **University-breakdown chart = raw email-domain grouping, no curated mapping.** Considered building a `davis.edu → "UC Davis"` lookup map. Dropped because (a) it's another file to maintain, (b) the raw domains are already informative for ops, (c) Phase 2 can swap this for a proper university column when we have a reason to.
- **2026-05-17** — **Aggregates computed in TS, not Postgres RPC.** Considered defining `get_admin_overview()` as a SQL function (migration). Skipped for now: dataset is small (~hundreds of users, low thousands of rides), single-file endpoint is easier to maintain, no extra migration. Triggered to swap to an RPC when total users + rides crosses ~50k OR the endpoint p95 latency hits >500ms — both far away.
- **2026-05-16** — **Email split into two paths:**
  - **Receiving** (someone emails `admin@tagorides.com` → lands in an inbox) is already handled by **GoDaddy** — Tarun set up the alias before this conversation. No code or extra setup needed.
  - **Sending** (Tago server emails AS `marketing@tagorides.com`) needs **Resend** (Slice 0.1). DNS records (DKIM/SPF) go in GoDaddy's DNS panel — Resend gives the exact values. Once verified, any `*@tagorides.com` address can be used in the From field.
  - **Cloudflare path dropped** — was an alternative for someone without email set up; Tarun has GoDaddy, so it's redundant.
- **2026-05-16** — Email composer (Slice 1.5) gets Gmail-style polish: TipTap rich text editor, From-address dropdown (config-driven allowlist of aliases), drag-drop PNG/JPG/PDF attachments (≤10MB each, ≤5 per campaign), Resend's batch send + webhook for opens/clicks. Reason: Tarun explicitly asked for "interface like sending the email like a regular gmail" with poster upload.
- **2026-05-16** — **One shared `admin@tagorides.com` account** instead of per-person `tarun@` / `hitesh@`. Trade-off accepted: audit log shows the shared account for every action, can't tell who did what without out-of-band records. Acceptable for a 1–3 person team that trusts each other. Trigger to switch to per-person accounts: ≥4 people OR compliance audit need OR per-role permission split. Password stored in shared password manager (1Password / Bitwarden / Apple Passwords).
- **2026-05-16** — Admin emails on `@tagorides.com`, not `@gmail.com`, to visually separate from `.edu` student users.
- **2026-05-16** — Allowlist-only auth for Phase 1; RBAC deferred to Phase 3. Reasoning: 1-person team right now; over-engineering permissions before there are roles to assign is premature.
- **2026-05-16** — Push + email + in-app banner all in Phase 1 (one campaign, three surfaces). Reasoning: cheap to do at the same time since the audience query is shared; defers later UX work.
- **2026-05-16** — Live ops view in Phase 1 (not Phase 2). Reasoning: most exciting CEO feature, uses existing Supabase Realtime + `<MapView>` infra, no new tech. ~1 dev day cost.
- **2026-05-16** — Uber-style hybrid in-app banner + push (foreground → banner, background → push). Reasoning: best UX, web already has `ForegroundPushToast`; iOS gains a thin extension.
- **2026-05-16** — Email via Resend (not Mailgun / SendGrid / Postmark). Reasoning: cleanest API, React Email integration, generous free tier ($0 / 3k emails/mo), supports DKIM out of the box.
- **2026-05-16** — Admin routes at `/admin/*` on the same web app, not a separate subdomain. Reasoning: reuses existing auth, no new deploy infra. If admin grows huge, can extract later.

---

## Blockers / open questions

(empty)

---

## Recent sessions

| Date | Tasks worked | One-line result |
|---|---|---|
| 2026-05-16 | Drafted `ADMIN_PLAN.md` + this progress file | Plan + tracker ready for Tarun review |
| 2026-05-16 | Locked decision: single shared `admin@tagorides.com` account, updated plan + decisions log | Phase 0 ready to kick off; Slice 0.1 (Cloudflare) is Tarun's next step |
| 2026-05-16 | Pivot: GoDaddy already handles receiving; rewrote Slice 0.1 around Resend + GoDaddy DNS. Expanded Slice 1.5 with Gmail-style composer (From dropdown, rich text, attachments) | Plan now accurate for Tarun's actual GoDaddy setup; ready for him to run Slice 0.1 |
| 2026-05-16 | Slice 0.1 done by Tarun: Resend → tagorides.com → DNS via GoDaddy → domain Verified. First API key was exposed in chat → instructed rotation. New key lives in EC2 `~/hich-web/.env` as `RESEND_API_KEY`. Plan updated with credentials-boundary table. | Phase 0 unblocked for code work; Slice 0.2 starts next |
| 2026-05-16 | Slice 0.2 code: migration 069 (`users.is_admin`), web `isValidEduEmail` + `isAdminEmail` accept `@tagorides.com`, web `database.ts` type adds `is_admin`, web `AuthGuard` bypass for admins, iOS `Validation.isAdminEmail` + `UserProfile.isAdmin` + `RootView.isProfileIncomplete` bypass. 17 new validation tests pass; 1069/1069 web tests green; lint clean; build clean; iOS sim builds. Local `.env`/`.env.dev` get `RESEND_API_KEY=` placeholder (Tarun pastes the rotated key locally). | Code complete, awaiting Tarun's dev signup test |
| 2026-05-17 | Slice 0.2 finalized: hardened the admin bypass to check `auth.user.email` BEFORE the profile-nil guard (fresh signups no longer trap at CreateProfile despite `is_admin` not yet set). Resolved email-template OTP-vs-link mismatch by copying prod's templates to dev. Dev SQL UPDATE flipped `is_admin=true` on `admin@tagorides.com`. End-to-end verified: signup → OTP confirm → sign-in lands directly on Home tab. 1081 tests green; lint+build clean. Encountered + diagnosed: Supabase rate limit (resolved via Resend custom SMTP); 8-digit-code-vs-link email confusion (resolved via template clone); CreateProfile trap (resolved via auth.user.email bypass). | Slice 0.2 done; ready for Slice 0.3 |
| 2026-05-17 | Slice 0.3 + 0.4 in one push: `adminAuth` middleware + `/api/admin/ping` endpoint; renamed old `/api/admin/*` operator-token-gated endpoints → `/api/ops/*` (4 endpoints: health, ghost-refunds GET+POST, payment-dunning); web `<AdminGuard>`, `<AdminLayout>` (sidebar + env badge + sign out), `<AdminHomePage>` (placeholder + ping probe + Phase 1 preview); `/admin/*` routes in `main.tsx`; "Open Admin Panel" button in ProfilePage. 6 new adminAuth tests cover all permission paths. 1090 tests green; lint+build clean. Phase 0 fully done. | Phase 0 complete; Phase 1 starts whenever Tarun says go |
| 2026-05-17 | Slice 1.1 code complete: migrations 070 (`users.last_active_at`) + 071 (`push_tokens.platform`); `validateJwt` bumps last_active_at (5-min throttle, fire-and-forget); web FCM upsert writes `platform: 'web'`; `GET /api/admin/metrics/overview` returns 12 KPIs + 3 chart series in one round trip (9 new tests, all pass); `useAdminOverview` React Query hook (5-min staleTime); `<AdminHomePage>` rewritten with 12 KPI cards in 4×3 grid + 3 Recharts charts; PostHog `admin_overview_loaded` event; recharts dependency added. 1112/1113 web tests pass (the 1 failure is parallel session's AddFundsPage Apple Pay test, unrelated). Lint clean. Server tsc clean. Client tsc + vite build are blocked by the parallel session's in-progress AddFundsPage.tsx — my files compile fine in isolation. | Awaiting Tarun: (a) apply migrations 070+071 to dev Supabase via SQL editor, (b) sign in to localhost:5173/admin and verify dashboard renders, (c) decide whether to commit before parallel session unblocks build |
| 2026-05-17 | Slice 1.1 verified end-to-end on dev. Encountered: dev `users.is_admin` had been reset to FALSE silently → dashboard 403'd. Re-flipped with one-line UPDATE, refresh worked. Lesson logged in `[adminAuth]` 403 diagnostic log (bundled with Slice 1.2). Pushed Slice 1.1 to origin/main (commit `c464659` on top of parallel session's sweep-up commit `8d6110b`). | Slice 1.1 live; Tarun's prod-parity dance pending. |
| 2026-05-17 | Slice 1.2 code complete: server `GET /api/admin/metrics/funnel` + `GET /api/admin/users/stuck` with shared `computeFunnelData()` helper, 14 new tests cover all 5 steps × 3 modes + drop-off math + pagination; web `useAdminFunnel` + `useAdminStuckUsers` hooks; `<FunnelPage>` at `/admin/funnel` with filter pills + horizontal bar viz + side-drawer drill-down; sidebar "Funnel" nav item added; `[adminAuth]` 403 diagnostic log bundled in. 1135/1135 tests pass; lint + build clean. No migrations needed (all data already on existing tables; email verification pulled via `auth.admin.listUsers`). | Awaiting Tarun: hard-refresh /admin → click Funnel → verify bars + filters + drawer, then decide push. |
| 2026-05-17 | Slice 1.2 verified on dev (funnel renders, filters work). Tarun requested plain-English explanations for every metric/step. Built `<InfoTooltip>` (small "i" trigger button → click-toggle popup, click-outside + Escape close, design-token styled). Wired into all 12 KPI cards + 3 charts on AdminHomePage + all 5 funnel steps on FunnelPage. Refactored funnel step from "whole card is a button" to "card + explicit 'See who's stuck here →' link" so the InfoTooltip "i" button doesn't sit inside a button (invalid HTML). 1135/1135 tests pass; lint + build clean. | Awaiting Tarun: tap any "i" on /admin or /admin/funnel to verify tooltip readability, then decide push. |
| 2026-05-17 | Tooltip overflow on funnel page step 5 → fixed by adding `align` prop to InfoTooltip (right anchored default for dashboard cards where "i" sits in card's top-right corner; left anchored for funnel where "i" sits right after a short title with empty card space to the right). Also rewrote `payment_or_vehicle` and `completed_first_ride` tooltips in plainer "what does this number tell me" framing per Tarun's feedback. Pushed Slice 1.2 + tooltips + tooltip-fix to origin/main as commits 80ca03e + 8dbb6c1 + 9fa5090. | Slice 1.2 + tooltips live. |
| 2026-05-17 | Slice 1.3a code complete: server `GET /api/admin/users/search` (ILIKE text fields + full-UUID exact-match + newest-first when empty) + `GET /api/admin/users/:id` (full profile + email_verified via auth admin + university derived + vehicle + routines/rides counts), 10 new tests. Web `<UsersPage>` (autofocus search, 250ms debounce, pagination, hover-row → /admin/users/:id) + `<UserDetailPage>` (header w/ avatar + 6-tab strip; Overview populated, other 5 stubbed). 1145/1145 tests pass; lint + build clean. No migrations needed. | Awaiting Tarun: hard-refresh /admin/users, search for any user, click through to the profile, sanity-check the Overview cards. |
| 2026-05-17 | Slice 1.3a verified + pushed (e2c4b3e). | Slice 1.3a live. |
| 2026-05-17 | Slice 1.3b code complete: 4 server endpoints under /api/admin/users/:id/ (rides / wallet / notifications / devices); 4 React Query hooks with `enabled` flag for lazy per-tab fetch; 4 tab components in UserDetailPage. Devices endpoint deliberately returns last 8 chars of push token only (never the full token — admins can identify a device without holding a sendable secret). 7 new tab tests pass. 1152/1152 tests; lint + build clean. No migrations. Surfaced 3 data gaps as follow-ups (notification delivery columns, push_token last_seen / app_version, live Stripe pending transfers). | Awaiting Tarun: hard-refresh /admin/users/:id, cycle through the 4 newly-real tabs, confirm data looks right for at least one user with rides + transactions. |
| 2026-05-17 | Slice 1.3b verified + pushed (6d255f1). Tiny iOS slice followed: `PushTokenRow` writes `platform: "ios"` so the Devices tab + iOS install rate KPI stop reading 'unknown'. Built + installed on iPhone 16 Pro; verified by Tarun ("its working"). | Slice 1.3b + iOS push platform write live. |
| 2026-05-17 | Slice 1.3c code complete (3 of 6 actions): migration 074 `admin_audit_log` + `server/lib/adminAudit.ts` helper + 3 POST endpoints under /api/admin/users/:id/actions/ (push / credit / override-onboarding) + GET /users/:id/audit; 15 new tests pass. Web: `<ActionsTab>` with 3 cards each carrying InfoTooltip + form + inline result + onSuccess invalidation of the relevant downstream queries; "Recent admin actions" list at the bottom (auto-refreshes after each mutation). New `adminPost<T>()` in `src/lib/admin/api.ts`. 1167/1167 tests; lint + build clean. Deferred to 1.3d (each needs its own design pass): Suspend account, Refund a ride, Force-reset password. | Awaiting Tarun: apply migration 074 to dev Supabase, hard-refresh /admin/users/:id, fire all 3 actions against a test user, verify the audit list updates + the downstream tabs reflect the change. |
| 2026-05-17 | Grant-Credit safety hardening triggered by Tarun's first-look feedback ("what if Stripe doesn't have enough? what if I mis-click?"). Added `GET /api/admin/stripe/balance` endpoint + shared `getAvailableStripeBalanceCents` helper. Credit endpoint now refuses with 409 INSUFFICIENT_STRIPE_BALANCE when the grant exceeds the platform balance, 503 STRIPE_BALANCE_UNAVAILABLE when Stripe is unreachable (errs safe). Web Grant Credit card renders the live Stripe balance under the amount field, disables submit if grant exceeds, and on submit opens a modal that echoes amount + email + reason + asks the admin to re-type the dollar amount for grants ≥$50. 3 new server tests cover the Stripe hard-block + outage paths (18/18 in adminActions.test.ts, 1170/1170 total). Lint + build clean. | Awaiting Tarun: same dev steps as 1.3c above, plus try (a) a $0.50 grant — straight confirm dialog, no re-type; (b) a $60 grant — re-type required; (c) entering more than the Stripe balance in dollars — submit button disables. |
| 2026-05-17 | Per Tarun's follow-up ("cap should be $50"): lowered MAX_CREDIT_CENTS from 10_000 * 100 → 50 * 100. Lowered re-type threshold from $50 → $25 so re-type still gates the upper half of the allowed range. Updated AMOUNT_TOO_LARGE error message + GrantCreditCard tooltip. Pushed (c3422da). | Cap-tightening live. |
| 2026-05-17 | Slice 1.5 (email broadcast) shipped. Migration 080 adds campaigns.channel + email_from. New /api/admin/campaigns/email + /from-addresses endpoints. Resend wrapper (server/lib/resend.ts) with From allowlist + concurrency-10 batch send. Audience preview becomes channel-aware (?channel=email switches opt-out column to email_marketing). Web: Push|Email tab switcher on /admin/campaigns, TipTap rich-text editor for email body, From dropdown. History list channel chip + channel-aware Duplicate. 6 new tests (25 total in adminCampaigns.test.ts; 1216/1216 overall). Lint + build clean. | Awaiting Tarun: apply migration 080, refresh /admin/campaigns, switch to Email tab, send yourself a test email with rich formatting via "Test to me". |
| 2026-05-17 | Slice 1.6 web half: extended `<ForegroundPushToast>` with a first-class `admin_broadcast` branch — 56×56 poster thumbnail, 10s dwell (vs 6s default), tap → `/c/<slug>` (or `/notifications` fallback). Until this slice, foreground browser tabs got a generic neutral toast via the `default` branch (no poster + wrong tap target + 6s dwell). Added 7 new tests covering poster present/absent, slug routing, fallback, 10s dwell, and verifying existing branches still work. 1223/1223 tests; lint + build clean. iOS half deferred — parks for the next iOS-touching slice. | Awaiting Tarun: refresh any web tab on Tago while signed in, fire a "Test to me" admin push from /admin/campaigns, verify the in-app banner shows with the poster + tap routes to /c/&lt;slug&gt;. |
| 2026-05-17 | Slice 1.7 (live ops view) shipped + corrected stale Campaigns caption. New `/api/admin/live/snapshot` returns active rides + 60-min event feed + name lookup, with 200/100 truncation flags (7 new server tests, 1230/1230 total). `useAdminLive` hook polls every 10s, paused when tab loses focus. `<LiveOpsPage>` mounted at `/admin/live` with a 3-section layout (Google Map left + event feed right + active-rides table below). Also patched CampaignsPage caption that still said "Email + in-app banner composers land in Slice 1.5 / 1.6" — both shipped, so caption now explains that push renders as an in-app banner for foregrounded users (no separate composer). Lint + build clean. | Awaiting Tarun: refresh /admin/live, verify markers appear for any currently-active ride, refresh the page and watch the events feed render — try cancelling a test ride and watching it show as a CANCELLED event in the feed within 10s. |
| 2026-05-17 | Slice 1.7b (live ops micro-management) shipped. Extended `/live/snapshot` to include `requested` rides + server-side `stuck_reason` computation (awaiting_driver / coordinating_long / driver_gps_stale / ride_long). New `POST /api/admin/rides/:rideId/cancel` flips status + notifies both parties + audits twice. Web LiveOpsPage: filter pills with counts, stuck-red highlights on map + table, click → right-side drawer with ride detail + 3 action sections (deep-link to profiles, quick-push composer per party prefilled with "checking in on your ride", force-cancel reason-gated). 17 new tests (10 live snapshot variants + 7 force-cancel paths); 1240/1240 total. Lint + build clean. No migrations. | Awaiting Tarun: refresh /admin/live, switch between filter pills, click any ride → verify drawer opens with deep-links / push composers / force-cancel — and try force-cancelling a test ride to verify both parties get the inbox notification + push. |
| 2026-05-17 | Slice 1.7c (stuck-step audiences in /admin/campaigns) shipped. 4 new audience types — `stuck_after_signup` / `stuck_after_verified_email` / `stuck_after_completed_profile` / `stuck_after_payment_or_vehicle` — that reuse `computeFunnelData` from funnel.ts so the audience size matches what the funnel chart shows on /admin/funnel. Hardcoded to `range='all', mode='both'` because marketing wants every parked user reachable. 5 new server tests; 1245/1245 total (- 2 SchedulePage flake that passes in isolation). Lint + build clean. No migration. | Awaiting Tarun: refresh /admin/campaigns, open the audience dropdown, pick "Stuck — set up, no first ride" and verify the count matches funnel's "completed first ride" drop-off step. Try a push or email to that audience as a verification. |
| 2026-05-17 | Funnel completed_first_ride bug fix (Tarun-reported). Riders who paid entirely from wallet balance (no saved default_payment_method_id — Tago allows this via wallet-first path in rides.ts) were clamped at max=2 instead of max=4. Fix: completed ride wins over cached payment column. 1 regression test pins wallet-only case. 1246/1246. Affected /admin/funnel chart + all 4 Slice 1.7c stuck-step audiences (wallet-only riders no longer nuisance-targeted). Lint + build clean. | (No QA needed — bug confirmed end-to-end by tests.) |
| 2026-05-17 | Slice 1.8 (global audit log page) shipped. New `GET /api/admin/audit-log` endpoint paginates every admin write, hydrates admin+target emails, validates UUID filters, returns distinct_actions for the client's dropdown. `<AuditLogPage>` at /admin/audit-log with filters card + table + collapsible JSON payload + prev/next pagination. 10 new server tests; 1256/1256 total. Lint + build clean. No migration. | Awaiting Tarun: refresh /admin/audit-log, verify recent admin actions (push sends, refunds, force-cancels) appear newest-first. Try filtering by action="force_cancel_ride" → only the cancel rows show. Click any target email → routes to the user's profile. |
| 2026-05-17 | Slice 1.9 (operational alerts panel) shipped. New `GET /api/admin/alerts` returns 5 categories (stuck_rides / failed_payments / unpaid_completed / negative_wallets / recent_suspensions) in one round trip, each capped at 50 with truncated flag. `<AlertsPage>` at /admin/alerts with 5 SectionCards (empty categories collapse to "All clear here"). Sidebar `Alerts` nav item gets a live red badge driven by the same hook (polls 30s, paused when tab loses focus). 11 new server tests; 1267/1267 total. Lint + build clean. No migration. | Awaiting Tarun: refresh /admin → see if the Alerts sidebar item gets a badge. Open /admin/alerts → expect the 5 sections. Try force-cancelling a stuck ride from Live ops → confirm the stuck-rides section count drops on the next poll. |
| 2026-05-17 | Slice 1.7d (online drivers + KPI strip on /admin/live) shipped. Extended /live/snapshot with online_drivers + available_driver_count + snoozed_driver_count using the matcher RPC's exact gate. Map renders idle drivers as green-pulse dots (busy drivers excluded to avoid double markers); top-right toggle hides/shows. Added 4-card KPI strip above filter pills (Active / Stuck / Online / Snoozed). 2 new server tests cover gate filtering + on_active_ride cross-ref. 1269/1269 total. Lint + build clean. No migration. | Awaiting Tarun: open /admin/live, expect the 4-card KPI strip. Open the app on a driver phone with is_online=true; within 5 min you should see a green dot at their GPS coords + the "Online drivers" KPI tick up. Toggle drivers offline → dot fades to /50 opacity and KPI drops on next poll. |
| 2026-05-17 | Slice 1.7e + 1.8b — two micro-slices bundled. **1.7e**: client-side free-text search above the active-rides table on /admin/live (matches rider/driver/origin/destination). Pure UI add, no server change, no tests needed. **1.8b**: `q=` server param on /admin/audit-log that runs `.or(action.ilike,payload::text.ilike)` so admins can find audit rows by campaign title, refund reason, or any payload string. Char-sanitization on q prevents PostgREST clause injection. 2 new server tests; 1271/1271 total. Lint + build clean. No migration. | Awaiting Tarun: /admin/live — type a rider's name into the new Search box and watch the table + map filter; /admin/audit-log — type a recent push title into Search and watch only those rows render. |
| 2026-05-17 | Slice 1.7f (manual driver-to-ride reassignment) shipped. New POST /api/admin/rides/:rideId/reassign with full validation chain (UUID / ride status / driver exists / is_driver / not suspended / not busy). Flips driver_id + status, notifies 2–3 parties via inbox + best-effort FCM, audits once per role. Pure DB write — does NOT touch matcher RPC. Web: ReassignCard in the LiveOps drawer with driver picker (idle online drivers only) + reason + two-step confirm. 13 new server tests cover every gate + happy paths. 1284/1284 total. Lint + build clean. No migration. | Awaiting Tarun: /admin/live → click a requested or accepted ride → drawer → "Reassign…" → pick an idle online driver → confirm. Expect: ride row's driver_id flips, new driver's phone gets a push, rider gets a "driver changed" push. If you try to reassign a mid-flight ride (active/coordinating) the card explains it's unavailable. |
| 2026-05-17 | Slice 1.7d-fix (online-gate alignment). Tarun caught that /admin/live showed 0 online drivers while the matcher Stage 1 fallback would still notify them. Root cause: my admin gate used 5-min ping freshness; the matcher uses a 7-day inclusion window (`clearStaleOnlineFlags` cron). Widened admin to 7 days; added `ping_stale` + `ping_age_ms` so the UI shows stale drivers with a warning instead of hiding them. Map dot uses dimmer fill + warning border for stale pings; KPI sub-line includes stale count; legend gains a "Online · stale ping" row. 1 new regression test. 1292/1292 total. Lint + build clean. No migration. | Awaiting Tarun: refresh /admin/live → testdriver should now appear as a green dot (possibly with warning border if ping is >5 min); ONLINE DRIVERS KPI should show 1 instead of 0. The notification problem itself is downstream — likely the dev iOS app GPS streaming + Firebase project. |
| 2026-05-17 | Slice 1.4d (campaign management) shipped. Migration 079 adds campaigns.recalled_at/reason/by. New POST /campaigns/:id/recall (deletes inbox rows + soft-deletes campaign + audit). Push endpoint supports test_to_self body field + filters by push_promos opt-outs (preview matches). Web: CampaignsPage gets a "Test to me" button + Past Campaigns history with Recall (confirm dialog) + Duplicate (pre-fills composer) actions. 7 new tests; 1210/1210 total. Lint + build clean. | Awaiting Tarun: apply migration 079, refresh /admin/campaigns, see past sends, recall one to wipe its inbox copies, duplicate one to pre-fill composer, "Test to me" to verify a send before going broad. |
| 2026-05-17 | Slice 1.4c (iOS NSE + in-app campaign detail sheet) shipped earlier today; server side has the mutable-content flag committed (78b533b). iOS half is gitignored. | Slice 1.4c live on iPhone 16 Pro. |
| 2026-05-17 | Slice 1.4b (marketing posters + tap-through detail page) shipped on top of 1.4. Migration 077 adds public.campaigns + campaign-posters storage bucket (admin-write, public-read). POST /campaigns/push now creates a campaigns row up-front, includes image_url + slug in FCM data, writes the same into notifications rows. New GET /api/admin/campaigns (paginated history) + GET /api/campaigns/:slug (public, no auth, returns title/body/poster/sent_at only). Web: poster upload in CampaignsPage (direct supabase.storage), preview in confirm dialog, new public /c/:slug page outside AuthGuard. firebase-messaging-sw.js renders the image in the OS notification banner + routes admin_broadcast taps to /c/<slug>. NotificationsPage shows the poster inline + deep-links on tap. 14 tests pass (added 1 new for poster flow). Lint + build clean. Slice 1.4c (iOS NSE for image banner + native deep link) is a separate iOS-touching slice. | Awaiting Tarun: apply migration 077 to dev Supabase, refresh /admin/campaigns, upload a test poster, send to your own user, watch the push arrive on web/Chrome with the image in the banner, tap it → lands on /c/<slug> with the full hero. |
| 2026-05-17 | Slice 1.4 (broadcast push composer) shipped. New /api/admin/campaigns/audience/preview + POST /push with 7 pre-baked audience types (all_users / all_drivers / all_riders / by_university / active_last_7d / active_last_30d / dormant_30d). AUDIENCE_DRIFT 409 if count moves between preview + send. Suspended users excluded. Notifications row per recipient + 1 broadcast audit row. 12 new tests (1203/1203 total). Web: /admin/campaigns page with composer + live audience side card + confirm-send modal (re-type recipient count required at ≥100). Lint + build clean. | Awaiting Tarun: hard-refresh /admin/campaigns, pick an audience, watch the live count update, send a test push to "all_drivers" or your own university, verify your test phone receives it + the Notifications tab on your user-detail picks up the inbox row. |
| 2026-05-17 | Slice 1.3e (Refund a ride) shipped. New POST /api/admin/rides/:rideId/refund with driver-overdraft soft-warn (409 with override flag). Two audit rows per refund (rider + driver). 10 new tests. Web: per-row Refund button on Rides tab + `<RefundRideDialog>` confirm modal. Refunded rides get a "refunded" chip. 1191/1191 tests; lint + build clean. **Slice 1.3 is now FULLY complete.** | Awaiting Tarun: hard-refresh a user's Rides tab, find a completed+paid ride, click Refund, confirm, verify rider's wallet credited + driver's debited + ride flips to refunded. Also try a refund where driver balance is below fare to see the overdraft warning. |
| 2026-05-17 | Slice 1.3d (Suspend + Force-reset password) shipped. Migration 076 adds `users.suspended_at` + `suspended_reason` + partial index. validateJwt enforces a SUSPENDED gate on every authenticated request (60s cache; admin bypass; skipped under VITEST). POST /actions/suspend (toggle + self-block + admin-block + reason required). POST /actions/reset-password (Supabase recovery generateLink). Web: 2 new cards in ActionsTab (Suspend with confirm dialog + Force-reset), "suspended" badge on header, "Suspended at/reason" rows in Overview Profile when applicable. 11 new tests (29 in adminActions.test.ts, 1181/1181 total). Lint + build clean. | Awaiting Tarun: apply migration 076 to dev Supabase, hard-refresh /admin/users/:id, test (a) Suspend a non-admin → user gets 403 on next API call; (b) Unsuspend → access restored; (c) try to suspend admin@tagorides.com → 400 CANNOT_SUSPEND_ADMIN; (d) Force-reset password → user receives Supabase recovery email. |

---

## How to use this file

1. **At the start of every admin-related work session:** read this file top-to-bottom. The summary table tells you what's next.
2. **The moment you start a slice:** flip its checkbox to `[~]` (in progress) and update the **Current focus** + **Next action** lines.
3. **The moment you finish a slice:** flip to `[x]` with a one-line note like `— shipped 2026-05-21, EC2 deployed`. Update the **Summary** counts.
4. **When you make a non-obvious decision:** prepend it to the Decisions log with today's date.
5. **If blocked:** add to Blockers section with one line of context. Move on to a parallel slice if possible.
6. **At session end:** append a row to Recent sessions table.

The point of all of this is: a teammate (or future-you, six weeks from now) should be able to open this file and immediately understand where the admin tool is, what's blocking it, and what to do next — without reading any code.
