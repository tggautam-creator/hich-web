TAGO — Senior Review Progress Tracker
======================================
Source: REVIEW.md + plan at
/Users/tarungautam/.claude/plans/you-are-a-senior-wobbly-thacker.md
Created: 2026-04-18

Working rules:
- Claude works on 1–2 items at a time, then STOPS for user review.
- Claude marks [~] IN PROGRESS while awaiting user review.
- Only after user tests + approves does Claude mark [x] DONE.
- Never push to GitHub before user tests locally (standing rule).

Statuses: [ ] TODO  |  [~] IN PROGRESS (awaiting user review)  |  [x] DONE  |  [!] BLOCKED

Currently in progress: R.17 ride-board filters + route-match, R.18 small cleanups
New initiative 2026-04-19: P2 — FRICTIONLESS DRIVER ONBOARDING (F1–F7, below)
Last updated: 2026-04-20


═══════════════════════════════════════════════════════
 P0 — CONCURRENCY & CORRECTNESS (ship first)
═══════════════════════════════════════════════════════

[x] R.1 — Atomic accept (REVIEW A1)
      Single conditional UPDATE with WHERE status='requested' AND driver_id IS NULL.
      Files: server/routes/rides.ts:1324, L937-977
      Verify: 2 concurrent accepts → one 200, one 409.

[x] R.2 — Wallet RPC wallet_apply_delta (REVIEW A2, A4)
      Supabase SQL function: balance update + transaction insert in one tx.
      Files: supabase/migrations/<ts>_wallet_apply_delta.sql (NEW),
             server/routes/wallet.ts, server/routes/stripeWebhook.ts
      Verify: insert-failure test rolls balance back; concurrent topups don't lose credit.

[x] R.3 — Charge-before-complete (REVIEW A3)
      chargeRideFare() first, then mark ride completed. Failure → payment_failed.
      Files: server/routes/rides.ts:2518-2585
      Verify: stubbed Stripe failure leaves ride in payment_failed, not completed.

[x] R.4 — Idempotency keys + middleware (REVIEW A6, A10, B2)
      Idempotency-Key header + request_idempotency table; return cached response.
      Files: server/middleware/idempotency.ts (NEW),
             supabase/migrations/<ts>_request_idempotency.sql (NEW),
             server/routes/rides.ts (request + scan-driver endpoints)
      Verify: replay POST returns cached response + no new rides row.

[x] R.5 — Driver single-active guard (REVIEW A7)
      Pre-check: SELECT 1 FROM rides WHERE driver_id=? AND status IN ('coordinating','active').
      Files: server/routes/rides.ts:833-1078
      Verify: driver with active single-rider ride cannot accept second single-rider ride.

[x] R.6 — Stale-closure fix (REVIEW A5, A9)
      posRef pattern for GPS interval; add missing deps / ref-ize state in WaitingRoom.
      Files: src/components/ride/RiderPickupPage.tsx:217-240,
             src/components/ride/WaitingRoom.tsx:91-135
      Verify: Vitest fake timers + state mutation → interval sends latest value.

[x] R.7 — Hard env validation at boot (REVIEW B1)
      Throw (not warn) on missing STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET,
      QR_HMAC_SECRET, FCM_SERVER_KEY.
      Files: server/env.ts:6-39
      Verify: boot with missing var exits non-zero.

[x] R.8 — GPS ping observability (REVIEW B3)
      Count consecutive failures, show "GPS weak" banner, emit PostHog event.
      Files: src/components/ride/DriverMultiRidePage.tsx:184-193
      Verify: offline device shows banner within N failures.

[x] R.9 — Standby-offer upsert guard (REVIEW A8)
      Conditional WHERE status != 'released'; inspect affected rows.
      Files: server/routes/rides.ts:937-977
      Verify: released offer does not resurrect on standby accept.

[x] R.10 — Scan-end broadcast ordering (REVIEW B2)
       Move broadcast + payment commit after rides UPDATE success.
       Files: server/routes/rides.ts:2845-2855
       Verify: forced UPDATE failure does not send broadcast or commit payment.


═══════════════════════════════════════════════════════
 P1 — UX POLISH (only after P0 lands)
═══════════════════════════════════════════════════════

[x] R.11 — CSS motion tokens + BottomSheet animation
       No framer-motion dep. Central src/lib/motion.ts tokens (durations,
       easings) so an iOS port (Capacitor / RN / native Swift) only needs to
       re-read the constants, not re-implement motion logic. BottomSheet
       slide-up + backdrop fade via Tailwind keyframes. Respect
       prefers-reduced-motion.
       Files: src/lib/motion.ts (NEW), src/components/ui/BottomSheet.tsx,
              tailwind.config.cjs

[x] R.12 — Driver card reveal on match
       Slide-up card: photo, car, plate, ETA, trust badges. CSS enter
       animation driven by the motion tokens.
       Files: src/components/ride/WaitingRoom.tsx

[x] R.13 — Animated car marker (bearing + tween)
       Rotate to bearing; useAnimatedPosition RAF hook tweens between GPS
       ticks (RAF works in WebView / Capacitor and has a direct Reanimated
       equivalent for a later RN port).
       Files: src/components/map/CarMarker.tsx, src/hooks/useAnimatedPosition.ts (NEW)

[x] R.14 — Live pickup ETA countdown
       Shared component driven by Distance Matrix polling.
       Files: src/components/ride/PickupEta.tsx (NEW),
              RiderPickupPage.tsx, DriverPickupPage.tsx

[~] R.15 — Trust badges component
       .edu verified, rides completed, avg rating.
       Files: src/components/ui/TrustBadges.tsx (NEW),
              WaitingRoom.tsx (driver card), ProfilePage.tsx

[~] R.16 — Rating + tip flow
       Tip picker ($1/$2/$5/custom) via tip_ride RPC (rider→driver atomic
       transfer, mirrors transfer_ride_fare). New endpoint POST
       /api/rides/:id/tip (rider-only, completed rides, one tip per ride).
       Files: supabase/migrations/046_tip_ride.sql (NEW),
              server/routes/rides.ts, src/components/ride/RateRidePage.tsx

[x] R.15 — Trust badges component  (done 2026-04-19)
[x] R.16 — Rating + tip flow  (done 2026-04-19)

═══════════════════════════════════════════════════════
 P1.5 — CTO RE-PLAN (2026-04-19)
 Target: 100-user campus beta, $0 infra, solo founder. Infra hardening must
 land before more UX polish so the Supabase free-tier 2GB/mo bandwidth cap
 doesn't bite during the beta window.
═══════════════════════════════════════════════════════

[x] R.19 — Rate-limit fix (done 2026-04-19, deployed to prod)
       server/app.ts — dev bypass (NODE_ENV !== 'production'), prod cap
       raised 200→2000 / 15 min, separate 600 / 5 min bucket for /gps-ping.

[x] R.23 — Bandwidth meter (done 2026-04-19, live at https://www.tagorides.com/api/admin/health)
       In-memory request+response-bytes counter; /api/admin/health endpoint
       returns monthly egress + top paths + Supabase-free-tier %.
       Gated by x-admin-token header (404 when ADMIN_TOKEN env unset).

[~] R.17 — Ride board filters + route-match  (awaiting user review)
       Time chips (Anytime/Today/This week) + Seats chips (Any/2+, hidden on
       riders tab). "Near you" pill on driver posts whose origin→dest polyline
       passes within a 5-min walk of the rider's current GPS (uses
       calculateInterceptPoint on 2-point polyline; upgrade input to full
       decoded polyline when available).
       Files: src/components/schedule/RideBoard.tsx,
              src/components/schedule/RideBoardCard.tsx

[~] R.18 — Small cleanups  (awaiting user review)
       WalletPage spinner → 4 shimmer rows matching transaction-card shape;
       RiderHomePage text-[2rem] → text-2xl. slide-down keyframe kept — it IS
       used (DriverHomePage banner + RideRequestNotification). REVIEW.md was
       stale on that point.
       Files: WalletPage.tsx, RiderHomePage.tsx


═══════════════════════════════════════════════════════
 CHANGE LOG
═══════════════════════════════════════════════════════
2026-04-18  Tracker created from senior review (REVIEW.md + plan file).
2026-04-18  R.11/R.12 scope updated: CSS-only motion (no framer-motion dep)
            so UX polish is portable to a future iOS app (Capacitor / React
            Native / native Swift). Central src/lib/motion.ts tokens carry
            the durations + easings; only the timing intent ports across
            platforms, not the motion implementation.
2026-04-19  CTO re-plan: target 100 users, $0 budget, solo. Interleave
            infra hardening (R.19 rate limit, R.23 bandwidth meter) ahead
            of R.17/R.18 polish. R.20 GPS-to-broadcast + R.21 in-memory
            ride cache parked behind an upgrade trigger (bandwidth meter
            crossing 60% of the Supabase free-tier 2GB/mo egress cap) —
            not worth the surface-area until we see real pressure.
2026-04-19  P2 initiative launched: frictionless driver onboarding
            (F1–F7). User decisions locked: platform-custody model,
            $100 wallet cap, ride-1 modal + wallet banner, 90-day
            ghost refund. Full architecture in REVIEW.md §D.


═══════════════════════════════════════════════════════
 P2 — FRICTIONLESS DRIVER ONBOARDING (CTO 2026-04-19)
 Goal: drivers Go Online immediately after car registration; bank
 connection deferred to first-ride moment. See REVIEW.md §D for full
 architecture, money-flow diagram, decisions, and risk table.
═══════════════════════════════════════════════════════

Decisions locked with user 2026-04-19:
  - Custody: charge rider → TAGO platform balance; credit driver's
    in-app wallet; transfer to Connect only on withdraw
  - Wallet cap: $100 hard gate (no broadcasts above cap until bank)
  - Prompt: modal after ride 1 + persistent WalletPage banner
  - Ghost driver: 90-day auto-refund, day-60 reminder email

Ship order is strict: F1 → F2 → F3 → F4 → F5 → F6 → F7.
Reason: F1 (kill transfer_data) is the keystone. Nothing else is safe
to ship until charges route to the platform balance instead of the
(possibly non-existent) Connect account.

[~] F1 — Rewire charge to platform (drop transfer_data.destination)
      chargeRideFare charges rider to TAGO balance; driver wallet
      credited atomically via wallet_apply_delta('ride_earning').
      transactions.type has NO CHECK constraint, so migration 049
      was not needed — 'ride_earning' is a plain string.
      Files: server/lib/stripeConnect.ts (dropped transfer_data +
             application_fee_amount + driverAccountId param),
             server/routes/rides.ts (3 charge sites: /end line 2612,
             /scan-driver line 2934, /retry-payment line 4210).
      Tests: src/test/server/stripeConnect.test.ts (NEW, 3 tests —
             no transfer_data on PaymentIntent; call signature has
             no driverAccountId; Stripe failure path).

[~] F2 — Remove hard bank gate on DriverHomePage
      Soft banner on WalletPage; bank-required dialog + hard gate
      removed from DriverHomePage. Go Online flow no longer requires
      Connect onboarding.
      Files: src/components/ride/DriverHomePage.tsx,
             src/components/ride/WalletPage.tsx
      Tests: DriverHomePage.test.tsx updated with F2 cases.

[~] F3 — $100 wallet cap hard gate
      Server: broadcast filter in rides.ts drops drivers at
      wallet_balance ≥ 10_000 AND !stripe_onboarding_complete.
      Client: Go Online disabled with "Add bank to keep earning"
      banner escalation.
      Files: server/routes/rides.ts (driverIds filter),
             src/components/ride/DriverHomePage.tsx
      Tests: DriverHomePage.test.tsx cap-gate cases.

[~] F4 — Ride-1 onboarding modal + persistent Wallet banner
      BankOnboardPrompt surfaces when a driver has wallet balance
      but no bank. Dismissal is balance-keyed — a new earning bump
      re-shows it. Analytics: bank_prompt_shown / dismissed /
      accepted. Wallet banner + Withdraw CTA wired on WalletPage.
      Files: src/components/ride/BankOnboardPrompt.tsx (NEW),
             src/components/ride/DriverHomePage.tsx,
             src/components/ride/WalletPage.tsx
      Tests: src/test/ride/BankOnboardPrompt.test.tsx (6 tests).

[~] F5 — Withdraw endpoint + WithdrawSheet UI
      POST /api/wallet/withdraw debits via wallet_apply_delta
      ('withdrawal'), creates stripe.transfers.create to Connect
      account, credit-backs on Stripe failure. Idempotency-Key
      required (reuses R.4 middleware).
      Files: server/routes/wallet.ts,
             src/components/ride/WithdrawSheet.tsx (NEW),
             src/components/ride/WalletPage.tsx
      Tests: wallet.test.ts (+8 endpoint cases),
             WithdrawSheet.test.tsx (NEW, 6 UI cases).

[~] F6 — Wallet UX for pre-bank drivers
      Header toggles "Pending payout" vs "Your balance" based on
      bank-linked state. Per-earning rows tagged "· Link bank to
      withdraw" for drivers without Connect. Riders + onboarded
      drivers see no tag.
      Files: src/components/ride/WalletPage.tsx
      Tests: WalletPage.test.tsx (+3 F6 label cases).

[~] F7 — Ghost-driver auto-refund (90-day)
      Daily job: day-60 sweep upserts ghost_refunds row with
      reminder_sent_at (email wiring deferred). Day-90 sweep creates
      idempotent Stripe refund → debits driver wallet via
      wallet_apply_delta('ghost_refund') → marks
      rides.payment_status='refunded_ghost_driver' → finalizes
      ghost_refunds row. UNIQUE(ride_id) prevents double-refund on
      replay. Admin queue at /api/admin/ghost-refunds and
      /api/admin/ghost-refunds/run.
      Files: supabase/migrations/049_ghost_refunds.sql (NEW),
             server/jobs/ghostRefund.ts (NEW),
             server/routes/admin.ts (extended),
             src/types/database.ts (ghost_refunds type).
      Tests: src/test/server/ghostRefund.test.ts (NEW, 5 cases —
             reminder upsert, onboarded-driver skip, day-90 refund
             full path, replay idempotency, Stripe failure bail).
      SQL migration: user must run 049 before deploying.
      Status: 1008 tests passing, lint clean, build clean.
              Awaiting user local test before push.
